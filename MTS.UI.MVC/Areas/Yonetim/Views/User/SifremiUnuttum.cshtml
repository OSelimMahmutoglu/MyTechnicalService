
@{
    ViewBag.Title = "SifremiUnuttum";
    Layout = "~/Areas/Yonetim/Views/Shared/_LayoutAdmin.cshtml";
}



<h2>Forget my Password</h2>


<div class="container">
    <div class="row">
        <div class="col-md-8">
            <div class="widget">
                <div class="widget-body">
                    <span class="text text-danger">@Html.ValidationSummary()</span>
                    @using (Html.BeginForm("SifremiUnuttum", "User", FormMethod.Post))
                    {
                        @Html.AntiForgeryToken()
                        <div class="row">
                            <div class="form-group col-sm-6">
                                <label>Kullanıcı Adı veya Email</label>
                                @Html.TextBox("name", null, new { @class = "form-control" })
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-sm-4">
                                <p>
                                    <input type="submit" class="btn theme-btn" value="Şifremi yolla" />
                                </p>
                            </div>
                        </div>
                    }

                </div>
                <!-- end: Widget -->
            </div>
            <!-- /REGISTER -->
        </div>
        <!-- WHY? -->
    </div>
</div>
